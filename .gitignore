node_modules/
.env
accounts.csv
*.pdf

# Salesforce JWT credentials — never commit private keys
*.pem
private_key.pem
certificate.pem

# Personal interview-prep material — never commit
*Playbook*.txt
*playbook*.txt
*SWE*Playbook*.txt

# Local-only Cursor rules — personal helpers, not project conventions
.cursor/rules/swe-playbook.mdc
.cursor/rules/response-style.mdc

# Local Python venv used for one-off image processing (not part of app)
.tools/
